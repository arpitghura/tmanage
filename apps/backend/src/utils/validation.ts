// src/utils/sanitization.ts
// import { RegisterUserInput } from '@/modules/auth/auth.schema';
// import { sanitize } from 'validator'; // Assuming you import sanitization function

// export function sanitizeUserInput(input: RegisterUserInput): RegisterUserInput {
//   return {
//     email: sanitize(input.email.trim()).normalizeEmail() as string,
//     password: sanitize(input.password.trim()) as string,
//     first_name: sanitize(input?.first_name?.trim()) as string,
//     last_name: sanitize(input?.last_name?.trim()) as string,
//   };
// }
