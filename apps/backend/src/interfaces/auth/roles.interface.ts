export enum Roles {
    Admin = 'admin',
    User = 'user',
    OrgHead = `org-head`,
    TeamLead = `team-lead`,
    Moderator = `moderator`,
    Member = `member`,
}

export const ADMIN = Roles.Admin;
export const USER = Roles.User;
export const ORG_HEAD = Roles.OrgHead;
export const TEAM_LEAD = Roles.TeamLead;
export const MODERATOR = Roles.Moderator;
export const MEMBER = Roles.Member;
